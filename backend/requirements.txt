# ── Rdzeń (wymagane) ──────────────────────────────────────────
fastapi>=0.100
uvicorn[standard]>=0.27
python-multipart>=0.0.9
httpx>=0.27
psutil>=5.9
aiofiles>=23.0
edge-tts>=7.2
soundfile>=0.12
pydub>=0.25
python-pptx>=0.6
PyMuPDF>=1.23
Pillow>=10.0
pyparsing>=3.0
ebooklib>=0.18
slowapi>=0.1
bcrypt>=4.0

# ── GPU / opcjonalne ──────────────────────────────────────────
# Instaluj przez: install.sh lub install.py
# torch: pip install torch==2.8.0+cu128 torchaudio==2.8.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# xtts: pip install coqui-tts>=0.25.0   (Python 3.11), lub TTS>=0.22.0 (Python <3.12)
# transkrypcja: pip install whisperx
# separacja: pip install demucs
# wideo AI: pip install diffusers>=0.38.0 transformers>=4.33.0 accelerate>=0.20.0
# opencv: pip install opencv-python-headless
# szumy: pip install deepfilternet
# UWAGA: audiocraft/MusicGen wymaga torch==2.1.0 — niekompatybilne z torch>=2.8
